# auto_socket_macros

Procedural macro implementation for [`auto_socket`](https://crates.io/crates/auto_socket).

Applications should depend on `auto_socket`, which re-exports all public macros. This implementation crate is published separately because Rust procedural macros must live in a `proc-macro` crate.

License: MIT
