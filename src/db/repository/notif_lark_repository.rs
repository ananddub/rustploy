// Auto-generated by sqlx-gen. Do not edit.

use crate::db::models::notif_lark::NotifLark;

#[derive(Debug, Clone, Default)]
pub struct PaginateNotifLarkParams {
    pub page: i64,
    pub per_page: i64,
}

#[derive(Debug, Clone)]
pub struct PaginationNotifLarkMeta {
    pub total: i64,
    pub per_page: i64,
    pub current_page: i64,
    pub last_page: i64,
    pub first_page: i64,
}

#[derive(Debug, Clone)]
pub struct PaginatedNotifLark {
    pub meta: PaginationNotifLarkMeta,
    pub data: Vec<NotifLark>,
}

#[derive(Debug, Clone, Default)]
pub struct InsertNotifLarkParams {
    pub webhook_url: String,
}

#[derive(Debug, Clone, Default)]
pub struct OverwriteNotifLarkParams {
    pub webhook_url: String,
}

#[derive(Debug, Clone, Default)]
pub struct UpdateNotifLarkParams {
    pub webhook_url: Option<String>,
}

pub struct NotifLarkRepository {
    pool: sqlx::SqlitePool,
}

impl NotifLarkRepository {
    pub fn new(pool: sqlx::SqlitePool) -> Self {
        Self { pool }
    }

    pub async fn get_all(&self) -> Result<Vec<NotifLark>, sqlx::Error> {
        sqlx::query_as::<_, NotifLark>(r#"SELECT * FROM notif_lark"#)
            .fetch_all(&self.pool)
            .await
    }

    pub async fn paginate(
        &self,
        params: &PaginateNotifLarkParams,
    ) -> Result<PaginatedNotifLark, sqlx::Error> {
        let total: i64 = sqlx::query_scalar(r#"SELECT COUNT(*) FROM notif_lark"#)
            .fetch_one(&self.pool)
            .await?;

        let per_page = params.per_page;
        let current_page = params.page;
        let last_page = (total + per_page - 1) / per_page;
        let offset = (current_page - 1) * per_page;

        let data = sqlx::query_as::<_, NotifLark>(
            r#"
            SELECT *
            FROM notif_lark
            LIMIT ? OFFSET ?
        "#,
        )
        .bind(per_page)
        .bind(offset)
        .fetch_all(&self.pool)
        .await?;

        Ok(PaginatedNotifLark {
            meta: PaginationNotifLarkMeta {
                total,
                per_page,
                current_page,
                last_page,
                first_page: 1,
            },
            data,
        })
    }

    pub async fn get(&self, id: i64) -> Result<Option<NotifLark>, sqlx::Error> {
        sqlx::query_as::<_, NotifLark>(
            r#"
            SELECT *
            FROM notif_lark
            WHERE id = ?
        "#,
        )
        .bind(id)
        .fetch_optional(&self.pool)
        .await
    }

    pub async fn insert(&self, params: &InsertNotifLarkParams) -> Result<NotifLark, sqlx::Error> {
        sqlx::query_as::<_, NotifLark>(
            r#"
            INSERT INTO notif_lark (webhook_url)
            VALUES (?)
            RETURNING *
        "#,
        )
        .bind(&params.webhook_url)
        .fetch_one(&self.pool)
        .await
    }

    pub async fn insert_many_transactionally(
        &self,
        entries: Vec<InsertNotifLarkParams>,
    ) -> Result<Vec<NotifLark>, sqlx::Error> {
        if entries.is_empty() {
            return Ok(Vec::new());
        }
        let mut tx = self.pool.begin().await?;

        let mut all_results = Vec::with_capacity(entries.len());
        let max_per_chunk = 65535 / 1usize;
        for chunk in entries.chunks(max_per_chunk) {
            let mut values_parts = Vec::with_capacity(chunk.len());
            for (row_idx, _) in chunk.iter().enumerate() {
                let base = row_idx * 1usize;
                let placeholders = vec!["?".to_string()];
                values_parts.push(format!("({})", placeholders.join(", ")));
            }
            let sql = format!(
                "INSERT INTO {} ({})\nVALUES {}\nRETURNING *",
                "notif_lark",
                "webhook_url",
                values_parts.join(", ")
            );
            let mut query = sqlx::query_as::<_, NotifLark>(&sql);
            for params in chunk {
                query = query.bind(&params.webhook_url);
            }
            let rows = query.fetch_all(&mut *tx).await?;
            all_results.extend(rows);
        }
        tx.commit().await?;

        Ok(all_results)
    }

    pub async fn overwrite(
        &self,
        id: i64,
        params: &OverwriteNotifLarkParams,
    ) -> Result<NotifLark, sqlx::Error> {
        sqlx::query_as::<_, NotifLark>(
            r#"
            UPDATE notif_lark
            SET
              webhook_url = ?
            WHERE id = ?
            RETURNING *
        "#,
        )
        .bind(&params.webhook_url)
        .bind(id)
        .fetch_one(&self.pool)
        .await
    }

    pub async fn update(
        &self,
        id: i64,
        params: &UpdateNotifLarkParams,
    ) -> Result<NotifLark, sqlx::Error> {
        sqlx::query_as::<_, NotifLark>(
            r#"
            UPDATE notif_lark
            SET
              webhook_url = COALESCE(?, webhook_url)
            WHERE id = ?
            RETURNING *
        "#,
        )
        .bind(&params.webhook_url)
        .bind(id)
        .fetch_one(&self.pool)
        .await
    }

    pub async fn delete(&self, id: i64) -> Result<(), sqlx::Error> {
        sqlx::query(
            r#"
            DELETE FROM notif_lark
            WHERE id = ?
        "#,
        )
        .bind(id)
        .execute(&self.pool)
        .await?;

        Ok(())
    }
}
