// Auto-generated by sqlx-gen. Do not edit.

use crate::db::models::libsql_dbs::LibsqlDb;

#[derive(Debug, Clone, Default)]
pub struct PaginateLibsqlDbParams {
    pub page: i64,
    pub per_page: i64,
}

#[derive(Debug, Clone)]
pub struct PaginationLibsqlDbMeta {
    pub total: i64,
    pub per_page: i64,
    pub current_page: i64,
    pub last_page: i64,
    pub first_page: i64,
}

#[derive(Debug, Clone)]
pub struct PaginatedLibsqlDb {
    pub meta: PaginationLibsqlDbMeta,
    pub data: Vec<LibsqlDb>,
}

#[derive(Debug, Clone, Default)]
pub struct InsertLibsqlDbParams {
    pub name: String,
    pub app_name: String,
    pub description: Option<String>,
    pub docker_image: Option<String>,
    pub database_user: String,
    pub database_password: String,
    pub sqld_node: Option<String>,
    pub sqld_primary_url: Option<String>,
    pub enable_namespaces: Option<i64>,
    pub external_port: Option<i64>,
    pub external_grpc_port: Option<i64>,
    pub external_admin_port: Option<i64>,
    pub command: Option<String>,
    pub args: Option<String>,
    pub env_var: Option<String>,
    pub memory_reservation: Option<String>,
    pub memory_limit: Option<String>,
    pub cpu_reservation: Option<String>,
    pub cpu_limit: Option<String>,
    pub replicas: Option<i64>,
    pub app_status: Option<String>,
    pub health_check_swarm: Option<String>,
    pub restart_policy_swarm: Option<String>,
    pub placement_swarm: Option<String>,
    pub update_config_swarm: Option<String>,
    pub rollback_config_swarm: Option<String>,
    pub mode_swarm: Option<String>,
    pub labels_swarm: Option<String>,
    pub network_swarm: Option<String>,
    pub endpoint_spec_swarm: Option<String>,
    pub ulimits_swarm: Option<String>,
    pub stop_grace_period_swarm: Option<i64>,
    pub environment_id: i64,
    pub server_id: Option<i64>,
    pub created_at: Option<i64>,
    pub updated_at: Option<i64>,
}

#[derive(Debug, Clone, Default)]
pub struct OverwriteLibsqlDbParams {
    pub name: String,
    pub app_name: String,
    pub description: Option<String>,
    pub docker_image: String,
    pub database_user: String,
    pub database_password: String,
    pub sqld_node: String,
    pub sqld_primary_url: Option<String>,
    pub enable_namespaces: i64,
    pub external_port: Option<i64>,
    pub external_grpc_port: Option<i64>,
    pub external_admin_port: Option<i64>,
    pub command: Option<String>,
    pub args: Option<String>,
    pub env_var: Option<String>,
    pub memory_reservation: Option<String>,
    pub memory_limit: Option<String>,
    pub cpu_reservation: Option<String>,
    pub cpu_limit: Option<String>,
    pub replicas: i64,
    pub app_status: String,
    pub health_check_swarm: Option<String>,
    pub restart_policy_swarm: Option<String>,
    pub placement_swarm: Option<String>,
    pub update_config_swarm: Option<String>,
    pub rollback_config_swarm: Option<String>,
    pub mode_swarm: Option<String>,
    pub labels_swarm: Option<String>,
    pub network_swarm: Option<String>,
    pub endpoint_spec_swarm: Option<String>,
    pub ulimits_swarm: Option<String>,
    pub stop_grace_period_swarm: Option<i64>,
    pub environment_id: i64,
    pub server_id: Option<i64>,
    pub created_at: i64,
    pub updated_at: i64,
}

#[derive(Debug, Clone, Default)]
pub struct UpdateLibsqlDbParams {
    pub name: Option<String>,
    pub app_name: Option<String>,
    pub description: Option<String>,
    pub docker_image: Option<String>,
    pub database_user: Option<String>,
    pub database_password: Option<String>,
    pub sqld_node: Option<String>,
    pub sqld_primary_url: Option<String>,
    pub enable_namespaces: Option<i64>,
    pub external_port: Option<i64>,
    pub external_grpc_port: Option<i64>,
    pub external_admin_port: Option<i64>,
    pub command: Option<String>,
    pub args: Option<String>,
    pub env_var: Option<String>,
    pub memory_reservation: Option<String>,
    pub memory_limit: Option<String>,
    pub cpu_reservation: Option<String>,
    pub cpu_limit: Option<String>,
    pub replicas: Option<i64>,
    pub app_status: Option<String>,
    pub health_check_swarm: Option<String>,
    pub restart_policy_swarm: Option<String>,
    pub placement_swarm: Option<String>,
    pub update_config_swarm: Option<String>,
    pub rollback_config_swarm: Option<String>,
    pub mode_swarm: Option<String>,
    pub labels_swarm: Option<String>,
    pub network_swarm: Option<String>,
    pub endpoint_spec_swarm: Option<String>,
    pub ulimits_swarm: Option<String>,
    pub stop_grace_period_swarm: Option<i64>,
    pub environment_id: Option<i64>,
    pub server_id: Option<i64>,
    pub created_at: Option<i64>,
    pub updated_at: Option<i64>,
}

pub struct LibsqlDbRepository {
    pool: sqlx::SqlitePool,
}

impl LibsqlDbRepository {
    pub fn new(pool: sqlx::SqlitePool) -> Self {
        Self { pool }
    }

    pub async fn get_all(&self) -> Result<Vec<LibsqlDb>, sqlx::Error> {
        sqlx::query_as::<_, LibsqlDb>(r#"SELECT * FROM libsql_dbs"#)
            .fetch_all(&self.pool)
            .await
    }

    pub async fn paginate(
        &self,
        params: &PaginateLibsqlDbParams,
    ) -> Result<PaginatedLibsqlDb, sqlx::Error> {
        let total: i64 = sqlx::query_scalar(r#"SELECT COUNT(*) FROM libsql_dbs"#)
            .fetch_one(&self.pool)
            .await?;

        let per_page = params.per_page;
        let current_page = params.page;
        let last_page = (total + per_page - 1) / per_page;
        let offset = (current_page - 1) * per_page;

        let data = sqlx::query_as::<_, LibsqlDb>(
            r#"
            SELECT *
            FROM libsql_dbs
            LIMIT ? OFFSET ?
        "#,
        )
        .bind(per_page)
        .bind(offset)
        .fetch_all(&self.pool)
        .await?;

        Ok(PaginatedLibsqlDb {
            meta: PaginationLibsqlDbMeta {
                total,
                per_page,
                current_page,
                last_page,
                first_page: 1,
            },
            data,
        })
    }

    pub async fn get(&self, id: String) -> Result<Option<LibsqlDb>, sqlx::Error> {
        sqlx::query_as::<_, LibsqlDb>(
            r#"
            SELECT *
            FROM libsql_dbs
            WHERE id = ?
        "#,
        )
        .bind(id)
        .fetch_optional(&self.pool)
        .await
    }

    pub async fn insert(&self, params: &InsertLibsqlDbParams) -> Result<LibsqlDb, sqlx::Error> {
        sqlx::query_as::<
            _,
            LibsqlDb,
        >(
                r#"
            INSERT INTO libsql_dbs (name, app_name, description, docker_image, database_user, database_password, sqld_node, sqld_primary_url, enable_namespaces, external_port, external_grpc_port, external_admin_port, command, args, env_var, memory_reservation, memory_limit, cpu_reservation, cpu_limit, replicas, app_status, health_check_swarm, restart_policy_swarm, placement_swarm, update_config_swarm, rollback_config_swarm, mode_swarm, labels_swarm, network_swarm, endpoint_spec_swarm, ulimits_swarm, stop_grace_period_swarm, environment_id, server_id, created_at, updated_at)
            VALUES (?, ?, ?, COALESCE(?, 'ghcr.io/tursodatabase/libsql-server:latest'), ?, ?, COALESCE(?, 'PRIMARY'), ?, COALESCE(?, 0), ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, COALESCE(?, 1), COALESCE(?, 'IDLE'), ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, COALESCE(?, strftime('%s', 'now')), COALESCE(?, strftime('%s', 'now')))
            RETURNING *
        "#,
            )
            .bind(&params.name)
            .bind(&params.app_name)
            .bind(&params.description)
            .bind(&params.docker_image)
            .bind(&params.database_user)
            .bind(&params.database_password)
            .bind(&params.sqld_node)
            .bind(&params.sqld_primary_url)
            .bind(&params.enable_namespaces)
            .bind(&params.external_port)
            .bind(&params.external_grpc_port)
            .bind(&params.external_admin_port)
            .bind(&params.command)
            .bind(&params.args)
            .bind(&params.env_var)
            .bind(&params.memory_reservation)
            .bind(&params.memory_limit)
            .bind(&params.cpu_reservation)
            .bind(&params.cpu_limit)
            .bind(&params.replicas)
            .bind(&params.app_status)
            .bind(&params.health_check_swarm)
            .bind(&params.restart_policy_swarm)
            .bind(&params.placement_swarm)
            .bind(&params.update_config_swarm)
            .bind(&params.rollback_config_swarm)
            .bind(&params.mode_swarm)
            .bind(&params.labels_swarm)
            .bind(&params.network_swarm)
            .bind(&params.endpoint_spec_swarm)
            .bind(&params.ulimits_swarm)
            .bind(&params.stop_grace_period_swarm)
            .bind(&params.environment_id)
            .bind(&params.server_id)
            .bind(&params.created_at)
            .bind(&params.updated_at)
            .fetch_one(&self.pool)
            .await
    }

    pub async fn insert_many_transactionally(
        &self,
        entries: Vec<InsertLibsqlDbParams>,
    ) -> Result<Vec<LibsqlDb>, sqlx::Error> {
        if entries.is_empty() {
            return Ok(Vec::new());
        }
        let mut tx = self.pool.begin().await?;

        let mut all_results = Vec::with_capacity(entries.len());
        let max_per_chunk = 65535 / 36usize;
        for chunk in entries.chunks(max_per_chunk) {
            let mut values_parts = Vec::with_capacity(chunk.len());
            for (row_idx, _) in chunk.iter().enumerate() {
                let base = row_idx * 36usize;
                let placeholders = vec![
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!(
                        "COALESCE(?, {})",
                        "'ghcr.io/tursodatabase/libsql-server:latest'"
                    ),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "'PRIMARY'"),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "0"),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "1"),
                    format!("COALESCE(?, {})", "'IDLE'"),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "strftime('%s', 'now')"),
                    format!("COALESCE(?, {})", "strftime('%s', 'now')"),
                ];
                values_parts.push(format!("({})", placeholders.join(", ")));
            }
            let sql = format!(
                "INSERT INTO {} ({})\nVALUES {}\nRETURNING *",
                "libsql_dbs",
                "name, app_name, description, docker_image, database_user, database_password, sqld_node, sqld_primary_url, enable_namespaces, external_port, external_grpc_port, external_admin_port, command, args, env_var, memory_reservation, memory_limit, cpu_reservation, cpu_limit, replicas, app_status, health_check_swarm, restart_policy_swarm, placement_swarm, update_config_swarm, rollback_config_swarm, mode_swarm, labels_swarm, network_swarm, endpoint_spec_swarm, ulimits_swarm, stop_grace_period_swarm, environment_id, server_id, created_at, updated_at",
                values_parts.join(", ")
            );
            let mut query = sqlx::query_as::<_, LibsqlDb>(sqlx::AssertSqlSafe(&*sql));
            for params in chunk {
                query = query.bind(&params.name);
                query = query.bind(&params.app_name);
                query = query.bind(&params.description);
                query = query.bind(&params.docker_image);
                query = query.bind(&params.database_user);
                query = query.bind(&params.database_password);
                query = query.bind(&params.sqld_node);
                query = query.bind(&params.sqld_primary_url);
                query = query.bind(&params.enable_namespaces);
                query = query.bind(&params.external_port);
                query = query.bind(&params.external_grpc_port);
                query = query.bind(&params.external_admin_port);
                query = query.bind(&params.command);
                query = query.bind(&params.args);
                query = query.bind(&params.env_var);
                query = query.bind(&params.memory_reservation);
                query = query.bind(&params.memory_limit);
                query = query.bind(&params.cpu_reservation);
                query = query.bind(&params.cpu_limit);
                query = query.bind(&params.replicas);
                query = query.bind(&params.app_status);
                query = query.bind(&params.health_check_swarm);
                query = query.bind(&params.restart_policy_swarm);
                query = query.bind(&params.placement_swarm);
                query = query.bind(&params.update_config_swarm);
                query = query.bind(&params.rollback_config_swarm);
                query = query.bind(&params.mode_swarm);
                query = query.bind(&params.labels_swarm);
                query = query.bind(&params.network_swarm);
                query = query.bind(&params.endpoint_spec_swarm);
                query = query.bind(&params.ulimits_swarm);
                query = query.bind(&params.stop_grace_period_swarm);
                query = query.bind(&params.environment_id);
                query = query.bind(&params.server_id);
                query = query.bind(&params.created_at);
                query = query.bind(&params.updated_at);
            }
            let rows = query.fetch_all(&mut *tx).await?;
            all_results.extend(rows);
        }
        tx.commit().await?;

        Ok(all_results)
    }

    pub async fn overwrite(
        &self,
        id: String,
        params: &OverwriteLibsqlDbParams,
    ) -> Result<LibsqlDb, sqlx::Error> {
        sqlx::query_as::<_, LibsqlDb>(
            r#"
            UPDATE libsql_dbs
            SET
              name = ?,
              app_name = ?,
              description = ?,
              docker_image = ?,
              database_user = ?,
              database_password = ?,
              sqld_node = ?,
              sqld_primary_url = ?,
              enable_namespaces = ?,
              external_port = ?,
              external_grpc_port = ?,
              external_admin_port = ?,
              command = ?,
              args = ?,
              env_var = ?,
              memory_reservation = ?,
              memory_limit = ?,
              cpu_reservation = ?,
              cpu_limit = ?,
              replicas = ?,
              app_status = ?,
              health_check_swarm = ?,
              restart_policy_swarm = ?,
              placement_swarm = ?,
              update_config_swarm = ?,
              rollback_config_swarm = ?,
              mode_swarm = ?,
              labels_swarm = ?,
              network_swarm = ?,
              endpoint_spec_swarm = ?,
              ulimits_swarm = ?,
              stop_grace_period_swarm = ?,
              environment_id = ?,
              server_id = ?,
              created_at = ?,
              updated_at = ?
            WHERE id = ?
            RETURNING *
        "#,
        )
        .bind(&params.name)
        .bind(&params.app_name)
        .bind(&params.description)
        .bind(&params.docker_image)
        .bind(&params.database_user)
        .bind(&params.database_password)
        .bind(&params.sqld_node)
        .bind(&params.sqld_primary_url)
        .bind(&params.enable_namespaces)
        .bind(&params.external_port)
        .bind(&params.external_grpc_port)
        .bind(&params.external_admin_port)
        .bind(&params.command)
        .bind(&params.args)
        .bind(&params.env_var)
        .bind(&params.memory_reservation)
        .bind(&params.memory_limit)
        .bind(&params.cpu_reservation)
        .bind(&params.cpu_limit)
        .bind(&params.replicas)
        .bind(&params.app_status)
        .bind(&params.health_check_swarm)
        .bind(&params.restart_policy_swarm)
        .bind(&params.placement_swarm)
        .bind(&params.update_config_swarm)
        .bind(&params.rollback_config_swarm)
        .bind(&params.mode_swarm)
        .bind(&params.labels_swarm)
        .bind(&params.network_swarm)
        .bind(&params.endpoint_spec_swarm)
        .bind(&params.ulimits_swarm)
        .bind(&params.stop_grace_period_swarm)
        .bind(&params.environment_id)
        .bind(&params.server_id)
        .bind(&params.created_at)
        .bind(&params.updated_at)
        .bind(id)
        .fetch_one(&self.pool)
        .await
    }

    pub async fn update(
        &self,
        id: String,
        params: &UpdateLibsqlDbParams,
    ) -> Result<LibsqlDb, sqlx::Error> {
        sqlx::query_as::<_, LibsqlDb>(
            r#"
            UPDATE libsql_dbs
            SET
              name = COALESCE(?, name),
              app_name = COALESCE(?, app_name),
              description = COALESCE(?, description),
              docker_image = COALESCE(?, docker_image),
              database_user = COALESCE(?, database_user),
              database_password = COALESCE(?, database_password),
              sqld_node = COALESCE(?, sqld_node),
              sqld_primary_url = COALESCE(?, sqld_primary_url),
              enable_namespaces = COALESCE(?, enable_namespaces),
              external_port = COALESCE(?, external_port),
              external_grpc_port = COALESCE(?, external_grpc_port),
              external_admin_port = COALESCE(?, external_admin_port),
              command = COALESCE(?, command),
              args = COALESCE(?, args),
              env_var = COALESCE(?, env_var),
              memory_reservation = COALESCE(?, memory_reservation),
              memory_limit = COALESCE(?, memory_limit),
              cpu_reservation = COALESCE(?, cpu_reservation),
              cpu_limit = COALESCE(?, cpu_limit),
              replicas = COALESCE(?, replicas),
              app_status = COALESCE(?, app_status),
              health_check_swarm = COALESCE(?, health_check_swarm),
              restart_policy_swarm = COALESCE(?, restart_policy_swarm),
              placement_swarm = COALESCE(?, placement_swarm),
              update_config_swarm = COALESCE(?, update_config_swarm),
              rollback_config_swarm = COALESCE(?, rollback_config_swarm),
              mode_swarm = COALESCE(?, mode_swarm),
              labels_swarm = COALESCE(?, labels_swarm),
              network_swarm = COALESCE(?, network_swarm),
              endpoint_spec_swarm = COALESCE(?, endpoint_spec_swarm),
              ulimits_swarm = COALESCE(?, ulimits_swarm),
              stop_grace_period_swarm = COALESCE(?, stop_grace_period_swarm),
              environment_id = COALESCE(?, environment_id),
              server_id = COALESCE(?, server_id),
              created_at = COALESCE(?, created_at),
              updated_at = COALESCE(?, updated_at)
            WHERE id = ?
            RETURNING *
        "#,
        )
        .bind(&params.name)
        .bind(&params.app_name)
        .bind(&params.description)
        .bind(&params.docker_image)
        .bind(&params.database_user)
        .bind(&params.database_password)
        .bind(&params.sqld_node)
        .bind(&params.sqld_primary_url)
        .bind(&params.enable_namespaces)
        .bind(&params.external_port)
        .bind(&params.external_grpc_port)
        .bind(&params.external_admin_port)
        .bind(&params.command)
        .bind(&params.args)
        .bind(&params.env_var)
        .bind(&params.memory_reservation)
        .bind(&params.memory_limit)
        .bind(&params.cpu_reservation)
        .bind(&params.cpu_limit)
        .bind(&params.replicas)
        .bind(&params.app_status)
        .bind(&params.health_check_swarm)
        .bind(&params.restart_policy_swarm)
        .bind(&params.placement_swarm)
        .bind(&params.update_config_swarm)
        .bind(&params.rollback_config_swarm)
        .bind(&params.mode_swarm)
        .bind(&params.labels_swarm)
        .bind(&params.network_swarm)
        .bind(&params.endpoint_spec_swarm)
        .bind(&params.ulimits_swarm)
        .bind(&params.stop_grace_period_swarm)
        .bind(&params.environment_id)
        .bind(&params.server_id)
        .bind(&params.created_at)
        .bind(&params.updated_at)
        .bind(id)
        .fetch_one(&self.pool)
        .await
    }

    pub async fn delete(&self, id: String) -> Result<(), sqlx::Error> {
        sqlx::query(
            r#"
            DELETE FROM libsql_dbs
            WHERE id = ?
        "#,
        )
        .bind(id)
        .execute(&self.pool)
        .await?;

        Ok(())
    }
}
