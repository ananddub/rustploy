// Auto-generated by sqlx-gen. Do not edit.

use crate::models::applications::Application;

#[derive(Debug, Clone, Default)]
pub struct PaginateApplicationParams {
    pub page: i64,
    pub per_page: i64,
}

#[derive(Debug, Clone)]
pub struct PaginationApplicationMeta {
    pub total: i64,
    pub per_page: i64,
    pub current_page: i64,
    pub last_page: i64,
    pub first_page: i64,
}

#[derive(Debug, Clone)]
pub struct PaginatedApplication {
    pub meta: PaginationApplicationMeta,
    pub data: Vec<Application>,
}

#[derive(Debug, Clone, Default)]
pub struct InsertApplicationParams {
    pub name: String,
    pub app_name: String,
    pub description: Option<String>,
    pub source_type: Option<String>,
    pub build_type: Option<String>,
    pub app_status: Option<String>,
    pub trigger_type: Option<String>,
    pub build_args: Option<String>,
    pub build_secrets: Option<String>,
    pub dockerfile: Option<String>,
    pub docker_context_path: Option<String>,
    pub docker_build_stage: Option<String>,
    pub publish_directory: Option<String>,
    pub is_static_spa: Option<i64>,
    pub create_env_file: Option<i64>,
    pub railpack_version: Option<String>,
    pub heroku_version: Option<String>,
    pub command: Option<String>,
    pub args: Option<String>,
    pub env_var: Option<String>,
    pub build_path: Option<String>,
    pub clean_cache: Option<i64>,
    pub drop_build_path: Option<String>,
    pub enable_submodules: Option<i64>,
    pub watch_paths: Option<String>,
    pub refresh_token: Option<String>,
    pub icon: Option<String>,
    pub memory_reservation: Option<String>,
    pub memory_limit: Option<String>,
    pub cpu_reservation: Option<String>,
    pub cpu_limit: Option<String>,
    pub replicas: Option<i64>,
    pub health_check_swarm: Option<String>,
    pub restart_policy_swarm: Option<String>,
    pub placement_swarm: Option<String>,
    pub update_config_swarm: Option<String>,
    pub rollback_config_swarm: Option<String>,
    pub mode_swarm: Option<String>,
    pub labels_swarm: Option<String>,
    pub network_swarm: Option<String>,
    pub endpoint_spec_swarm: Option<String>,
    pub ulimits_swarm: Option<String>,
    pub stop_grace_period_swarm: Option<i64>,
    pub repository: Option<String>,
    pub owner: Option<String>,
    pub branch: Option<String>,
    pub auto_deploy: Option<i64>,
    pub gitlab_project_id: Option<i64>,
    pub gitlab_repository: Option<String>,
    pub gitlab_owner: Option<String>,
    pub gitlab_branch: Option<String>,
    pub gitlab_build_path: Option<String>,
    pub gitlab_path_namespace: Option<String>,
    pub gitea_repository: Option<String>,
    pub gitea_owner: Option<String>,
    pub gitea_branch: Option<String>,
    pub gitea_build_path: Option<String>,
    pub bitbucket_repository: Option<String>,
    pub bitbucket_repository_slug: Option<String>,
    pub bitbucket_owner: Option<String>,
    pub bitbucket_branch: Option<String>,
    pub bitbucket_build_path: Option<String>,
    pub docker_image: Option<String>,
    pub docker_username: Option<String>,
    pub docker_password: Option<String>,
    pub registry_url: Option<String>,
    pub custom_git_url: Option<String>,
    pub custom_git_branch: Option<String>,
    pub custom_git_build_path: Option<String>,
    pub custom_git_ssh_key_id: Option<i64>,
    pub preview_env: Option<String>,
    pub preview_build_args: Option<String>,
    pub preview_build_secrets: Option<String>,
    pub preview_labels: Option<String>,
    pub preview_wildcard: Option<String>,
    pub preview_port: Option<i64>,
    pub preview_https: Option<i64>,
    pub preview_path: Option<String>,
    pub preview_certificate_type: Option<String>,
    pub preview_custom_cert_resolver: Option<String>,
    pub preview_limit: Option<i64>,
    pub is_preview_deployments_active: Option<i64>,
    pub preview_require_collaborator_permissions: Option<i64>,
    pub rollback_active: Option<i64>,
    pub environment_id: i64,
    pub server_id: Option<i64>,
    pub build_server_id: Option<i64>,
    pub registry_id: Option<i64>,
    pub rollback_registry_id: Option<i64>,
    pub build_registry_id: Option<i64>,
    pub github_provider_id: Option<i64>,
    pub gitlab_provider_id: Option<i64>,
    pub gitea_provider_id: Option<i64>,
    pub bitbucket_provider_id: Option<i64>,
    pub created_at: Option<i64>,
    pub updated_at: Option<i64>,
}

#[derive(Debug, Clone, Default)]
pub struct OverwriteApplicationParams {
    pub name: String,
    pub app_name: String,
    pub description: Option<String>,
    pub source_type: String,
    pub build_type: String,
    pub app_status: String,
    pub trigger_type: String,
    pub build_args: Option<String>,
    pub build_secrets: Option<String>,
    pub dockerfile: Option<String>,
    pub docker_context_path: Option<String>,
    pub docker_build_stage: Option<String>,
    pub publish_directory: Option<String>,
    pub is_static_spa: Option<i64>,
    pub create_env_file: i64,
    pub railpack_version: Option<String>,
    pub heroku_version: Option<String>,
    pub command: Option<String>,
    pub args: Option<String>,
    pub env_var: Option<String>,
    pub build_path: Option<String>,
    pub clean_cache: i64,
    pub drop_build_path: Option<String>,
    pub enable_submodules: i64,
    pub watch_paths: Option<String>,
    pub refresh_token: Option<String>,
    pub icon: Option<String>,
    pub memory_reservation: Option<String>,
    pub memory_limit: Option<String>,
    pub cpu_reservation: Option<String>,
    pub cpu_limit: Option<String>,
    pub replicas: i64,
    pub health_check_swarm: Option<String>,
    pub restart_policy_swarm: Option<String>,
    pub placement_swarm: Option<String>,
    pub update_config_swarm: Option<String>,
    pub rollback_config_swarm: Option<String>,
    pub mode_swarm: Option<String>,
    pub labels_swarm: Option<String>,
    pub network_swarm: Option<String>,
    pub endpoint_spec_swarm: Option<String>,
    pub ulimits_swarm: Option<String>,
    pub stop_grace_period_swarm: Option<i64>,
    pub repository: Option<String>,
    pub owner: Option<String>,
    pub branch: Option<String>,
    pub auto_deploy: Option<i64>,
    pub gitlab_project_id: Option<i64>,
    pub gitlab_repository: Option<String>,
    pub gitlab_owner: Option<String>,
    pub gitlab_branch: Option<String>,
    pub gitlab_build_path: Option<String>,
    pub gitlab_path_namespace: Option<String>,
    pub gitea_repository: Option<String>,
    pub gitea_owner: Option<String>,
    pub gitea_branch: Option<String>,
    pub gitea_build_path: Option<String>,
    pub bitbucket_repository: Option<String>,
    pub bitbucket_repository_slug: Option<String>,
    pub bitbucket_owner: Option<String>,
    pub bitbucket_branch: Option<String>,
    pub bitbucket_build_path: Option<String>,
    pub docker_image: Option<String>,
    pub docker_username: Option<String>,
    pub docker_password: Option<String>,
    pub registry_url: Option<String>,
    pub custom_git_url: Option<String>,
    pub custom_git_branch: Option<String>,
    pub custom_git_build_path: Option<String>,
    pub custom_git_ssh_key_id: Option<i64>,
    pub preview_env: Option<String>,
    pub preview_build_args: Option<String>,
    pub preview_build_secrets: Option<String>,
    pub preview_labels: Option<String>,
    pub preview_wildcard: Option<String>,
    pub preview_port: Option<i64>,
    pub preview_https: i64,
    pub preview_path: Option<String>,
    pub preview_certificate_type: String,
    pub preview_custom_cert_resolver: Option<String>,
    pub preview_limit: Option<i64>,
    pub is_preview_deployments_active: i64,
    pub preview_require_collaborator_permissions: i64,
    pub rollback_active: i64,
    pub environment_id: i64,
    pub server_id: Option<i64>,
    pub build_server_id: Option<i64>,
    pub registry_id: Option<i64>,
    pub rollback_registry_id: Option<i64>,
    pub build_registry_id: Option<i64>,
    pub github_provider_id: Option<i64>,
    pub gitlab_provider_id: Option<i64>,
    pub gitea_provider_id: Option<i64>,
    pub bitbucket_provider_id: Option<i64>,
    pub created_at: i64,
    pub updated_at: i64,
}

#[derive(Debug, Clone, Default)]
pub struct UpdateApplicationParams {
    pub name: Option<String>,
    pub app_name: Option<String>,
    pub description: Option<String>,
    pub source_type: Option<String>,
    pub build_type: Option<String>,
    pub app_status: Option<String>,
    pub trigger_type: Option<String>,
    pub build_args: Option<String>,
    pub build_secrets: Option<String>,
    pub dockerfile: Option<String>,
    pub docker_context_path: Option<String>,
    pub docker_build_stage: Option<String>,
    pub publish_directory: Option<String>,
    pub is_static_spa: Option<i64>,
    pub create_env_file: Option<i64>,
    pub railpack_version: Option<String>,
    pub heroku_version: Option<String>,
    pub command: Option<String>,
    pub args: Option<String>,
    pub env_var: Option<String>,
    pub build_path: Option<String>,
    pub clean_cache: Option<i64>,
    pub drop_build_path: Option<String>,
    pub enable_submodules: Option<i64>,
    pub watch_paths: Option<String>,
    pub refresh_token: Option<String>,
    pub icon: Option<String>,
    pub memory_reservation: Option<String>,
    pub memory_limit: Option<String>,
    pub cpu_reservation: Option<String>,
    pub cpu_limit: Option<String>,
    pub replicas: Option<i64>,
    pub health_check_swarm: Option<String>,
    pub restart_policy_swarm: Option<String>,
    pub placement_swarm: Option<String>,
    pub update_config_swarm: Option<String>,
    pub rollback_config_swarm: Option<String>,
    pub mode_swarm: Option<String>,
    pub labels_swarm: Option<String>,
    pub network_swarm: Option<String>,
    pub endpoint_spec_swarm: Option<String>,
    pub ulimits_swarm: Option<String>,
    pub stop_grace_period_swarm: Option<i64>,
    pub repository: Option<String>,
    pub owner: Option<String>,
    pub branch: Option<String>,
    pub auto_deploy: Option<i64>,
    pub gitlab_project_id: Option<i64>,
    pub gitlab_repository: Option<String>,
    pub gitlab_owner: Option<String>,
    pub gitlab_branch: Option<String>,
    pub gitlab_build_path: Option<String>,
    pub gitlab_path_namespace: Option<String>,
    pub gitea_repository: Option<String>,
    pub gitea_owner: Option<String>,
    pub gitea_branch: Option<String>,
    pub gitea_build_path: Option<String>,
    pub bitbucket_repository: Option<String>,
    pub bitbucket_repository_slug: Option<String>,
    pub bitbucket_owner: Option<String>,
    pub bitbucket_branch: Option<String>,
    pub bitbucket_build_path: Option<String>,
    pub docker_image: Option<String>,
    pub docker_username: Option<String>,
    pub docker_password: Option<String>,
    pub registry_url: Option<String>,
    pub custom_git_url: Option<String>,
    pub custom_git_branch: Option<String>,
    pub custom_git_build_path: Option<String>,
    pub custom_git_ssh_key_id: Option<i64>,
    pub preview_env: Option<String>,
    pub preview_build_args: Option<String>,
    pub preview_build_secrets: Option<String>,
    pub preview_labels: Option<String>,
    pub preview_wildcard: Option<String>,
    pub preview_port: Option<i64>,
    pub preview_https: Option<i64>,
    pub preview_path: Option<String>,
    pub preview_certificate_type: Option<String>,
    pub preview_custom_cert_resolver: Option<String>,
    pub preview_limit: Option<i64>,
    pub is_preview_deployments_active: Option<i64>,
    pub preview_require_collaborator_permissions: Option<i64>,
    pub rollback_active: Option<i64>,
    pub environment_id: Option<i64>,
    pub server_id: Option<i64>,
    pub build_server_id: Option<i64>,
    pub registry_id: Option<i64>,
    pub rollback_registry_id: Option<i64>,
    pub build_registry_id: Option<i64>,
    pub github_provider_id: Option<i64>,
    pub gitlab_provider_id: Option<i64>,
    pub gitea_provider_id: Option<i64>,
    pub bitbucket_provider_id: Option<i64>,
    pub created_at: Option<i64>,
    pub updated_at: Option<i64>,
}

pub struct ApplicationRepository {
    pool: sqlx::SqlitePool,
}

impl ApplicationRepository {
    pub fn new(pool: sqlx::SqlitePool) -> Self {
        Self { pool }
    }

    pub async fn get_all(&self) -> Result<Vec<Application>, sqlx::Error> {
        sqlx::query_as::<_, Application>(r#"SELECT * FROM applications"#)
            .fetch_all(&self.pool)
            .await
    }

    pub async fn paginate(
        &self,
        params: &PaginateApplicationParams,
    ) -> Result<PaginatedApplication, sqlx::Error> {
        let total: i64 = sqlx::query_scalar(r#"SELECT COUNT(*) FROM applications"#)
            .fetch_one(&self.pool)
            .await?;

        let per_page = params.per_page;
        let current_page = params.page;
        let last_page = (total + per_page - 1) / per_page;
        let offset = (current_page - 1) * per_page;

        let data = sqlx::query_as::<_, Application>(
            r#"
            SELECT *
            FROM applications
            LIMIT ? OFFSET ?
        "#,
        )
        .bind(per_page)
        .bind(offset)
        .fetch_all(&self.pool)
        .await?;

        Ok(PaginatedApplication {
            meta: PaginationApplicationMeta {
                total,
                per_page,
                current_page,
                last_page,
                first_page: 1,
            },
            data,
        })
    }

    pub async fn get(&self, id: String) -> Result<Option<Application>, sqlx::Error> {
        sqlx::query_as::<_, Application>(
            r#"
            SELECT *
            FROM applications
            WHERE id = ?
        "#,
        )
        .bind(id)
        .fetch_optional(&self.pool)
        .await
    }

    pub async fn insert(
        &self,
        params: &InsertApplicationParams,
    ) -> Result<Application, sqlx::Error> {
        sqlx::query_as::<
            _,
            Application,
        >(
                r#"
            INSERT INTO applications (name, app_name, description, source_type, build_type, app_status, trigger_type, build_args, build_secrets, dockerfile, docker_context_path, docker_build_stage, publish_directory, is_static_spa, create_env_file, railpack_version, heroku_version, command, args, env_var, build_path, clean_cache, drop_build_path, enable_submodules, watch_paths, refresh_token, icon, memory_reservation, memory_limit, cpu_reservation, cpu_limit, replicas, health_check_swarm, restart_policy_swarm, placement_swarm, update_config_swarm, rollback_config_swarm, mode_swarm, labels_swarm, network_swarm, endpoint_spec_swarm, ulimits_swarm, stop_grace_period_swarm, repository, owner, branch, auto_deploy, gitlab_project_id, gitlab_repository, gitlab_owner, gitlab_branch, gitlab_build_path, gitlab_path_namespace, gitea_repository, gitea_owner, gitea_branch, gitea_build_path, bitbucket_repository, bitbucket_repository_slug, bitbucket_owner, bitbucket_branch, bitbucket_build_path, docker_image, docker_username, docker_password, registry_url, custom_git_url, custom_git_branch, custom_git_build_path, custom_git_ssh_key_id, preview_env, preview_build_args, preview_build_secrets, preview_labels, preview_wildcard, preview_port, preview_https, preview_path, preview_certificate_type, preview_custom_cert_resolver, preview_limit, is_preview_deployments_active, preview_require_collaborator_permissions, rollback_active, environment_id, server_id, build_server_id, registry_id, rollback_registry_id, build_registry_id, github_provider_id, gitlab_provider_id, gitea_provider_id, bitbucket_provider_id, created_at, updated_at)
            VALUES (?, ?, ?, COALESCE(?, 'GITHUB'), COALESCE(?, 'NIXPACKS'), COALESCE(?, 'IDLE'), COALESCE(?, 'PUSH'), ?, ?, COALESCE(?, 'Dockerfile'), ?, ?, ?, COALESCE(?, 0), COALESCE(?, 1), COALESCE(?, '0.15.4'), COALESCE(?, '24'), ?, ?, ?, COALESCE(?, '/'), COALESCE(?, 0), ?, COALESCE(?, 0), ?, ?, ?, ?, ?, ?, ?, COALESCE(?, 1), ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, COALESCE(?, 1), ?, ?, ?, ?, COALESCE(?, '/'), ?, ?, ?, ?, COALESCE(?, '/'), ?, ?, ?, ?, COALESCE(?, '/'), ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, COALESCE(?, 3000), COALESCE(?, 0), COALESCE(?, '/'), COALESCE(?, 'NONE'), ?, COALESCE(?, 3), COALESCE(?, 0), COALESCE(?, 1), COALESCE(?, 0), ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, COALESCE(?, strftime('%s', 'now')), COALESCE(?, strftime('%s', 'now')))
            RETURNING *
        "#,
            )
            .bind(&params.name)
            .bind(&params.app_name)
            .bind(&params.description)
            .bind(&params.source_type)
            .bind(&params.build_type)
            .bind(&params.app_status)
            .bind(&params.trigger_type)
            .bind(&params.build_args)
            .bind(&params.build_secrets)
            .bind(&params.dockerfile)
            .bind(&params.docker_context_path)
            .bind(&params.docker_build_stage)
            .bind(&params.publish_directory)
            .bind(&params.is_static_spa)
            .bind(&params.create_env_file)
            .bind(&params.railpack_version)
            .bind(&params.heroku_version)
            .bind(&params.command)
            .bind(&params.args)
            .bind(&params.env_var)
            .bind(&params.build_path)
            .bind(&params.clean_cache)
            .bind(&params.drop_build_path)
            .bind(&params.enable_submodules)
            .bind(&params.watch_paths)
            .bind(&params.refresh_token)
            .bind(&params.icon)
            .bind(&params.memory_reservation)
            .bind(&params.memory_limit)
            .bind(&params.cpu_reservation)
            .bind(&params.cpu_limit)
            .bind(&params.replicas)
            .bind(&params.health_check_swarm)
            .bind(&params.restart_policy_swarm)
            .bind(&params.placement_swarm)
            .bind(&params.update_config_swarm)
            .bind(&params.rollback_config_swarm)
            .bind(&params.mode_swarm)
            .bind(&params.labels_swarm)
            .bind(&params.network_swarm)
            .bind(&params.endpoint_spec_swarm)
            .bind(&params.ulimits_swarm)
            .bind(&params.stop_grace_period_swarm)
            .bind(&params.repository)
            .bind(&params.owner)
            .bind(&params.branch)
            .bind(&params.auto_deploy)
            .bind(&params.gitlab_project_id)
            .bind(&params.gitlab_repository)
            .bind(&params.gitlab_owner)
            .bind(&params.gitlab_branch)
            .bind(&params.gitlab_build_path)
            .bind(&params.gitlab_path_namespace)
            .bind(&params.gitea_repository)
            .bind(&params.gitea_owner)
            .bind(&params.gitea_branch)
            .bind(&params.gitea_build_path)
            .bind(&params.bitbucket_repository)
            .bind(&params.bitbucket_repository_slug)
            .bind(&params.bitbucket_owner)
            .bind(&params.bitbucket_branch)
            .bind(&params.bitbucket_build_path)
            .bind(&params.docker_image)
            .bind(&params.docker_username)
            .bind(&params.docker_password)
            .bind(&params.registry_url)
            .bind(&params.custom_git_url)
            .bind(&params.custom_git_branch)
            .bind(&params.custom_git_build_path)
            .bind(&params.custom_git_ssh_key_id)
            .bind(&params.preview_env)
            .bind(&params.preview_build_args)
            .bind(&params.preview_build_secrets)
            .bind(&params.preview_labels)
            .bind(&params.preview_wildcard)
            .bind(&params.preview_port)
            .bind(&params.preview_https)
            .bind(&params.preview_path)
            .bind(&params.preview_certificate_type)
            .bind(&params.preview_custom_cert_resolver)
            .bind(&params.preview_limit)
            .bind(&params.is_preview_deployments_active)
            .bind(&params.preview_require_collaborator_permissions)
            .bind(&params.rollback_active)
            .bind(&params.environment_id)
            .bind(&params.server_id)
            .bind(&params.build_server_id)
            .bind(&params.registry_id)
            .bind(&params.rollback_registry_id)
            .bind(&params.build_registry_id)
            .bind(&params.github_provider_id)
            .bind(&params.gitlab_provider_id)
            .bind(&params.gitea_provider_id)
            .bind(&params.bitbucket_provider_id)
            .bind(&params.created_at)
            .bind(&params.updated_at)
            .fetch_one(&self.pool)
            .await
    }

    pub async fn insert_many_transactionally(
        &self,
        entries: Vec<InsertApplicationParams>,
    ) -> Result<Vec<Application>, sqlx::Error> {
        if entries.is_empty() {
            return Ok(Vec::new());
        }
        let mut tx = self.pool.begin().await?;

        let mut all_results = Vec::with_capacity(entries.len());
        let max_per_chunk = 65535 / 96usize;
        for chunk in entries.chunks(max_per_chunk) {
            let mut values_parts = Vec::with_capacity(chunk.len());
            for (row_idx, _) in chunk.iter().enumerate() {
                let base = row_idx * 96usize;
                let placeholders = vec![
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "'GITHUB'"),
                    format!("COALESCE(?, {})", "'NIXPACKS'"),
                    format!("COALESCE(?, {})", "'IDLE'"),
                    format!("COALESCE(?, {})", "'PUSH'"),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "'Dockerfile'"),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "0"),
                    format!("COALESCE(?, {})", "1"),
                    format!("COALESCE(?, {})", "'0.15.4'"),
                    format!("COALESCE(?, {})", "'24'"),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "'/'"),
                    format!("COALESCE(?, {})", "0"),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "0"),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "1"),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "1"),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "'/'"),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "'/'"),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "'/'"),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "3000"),
                    format!("COALESCE(?, {})", "0"),
                    format!("COALESCE(?, {})", "'/'"),
                    format!("COALESCE(?, {})", "'NONE'"),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "3"),
                    format!("COALESCE(?, {})", "0"),
                    format!("COALESCE(?, {})", "1"),
                    format!("COALESCE(?, {})", "0"),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    "?".to_string(),
                    format!("COALESCE(?, {})", "strftime('%s', 'now')"),
                    format!("COALESCE(?, {})", "strftime('%s', 'now')"),
                ];
                values_parts.push(format!("({})", placeholders.join(", ")));
            }
            let sql = format!(
                "INSERT INTO {} ({})\nVALUES {}\nRETURNING *",
                "applications",
                "name, app_name, description, source_type, build_type, app_status, trigger_type, build_args, build_secrets, dockerfile, docker_context_path, docker_build_stage, publish_directory, is_static_spa, create_env_file, railpack_version, heroku_version, command, args, env_var, build_path, clean_cache, drop_build_path, enable_submodules, watch_paths, refresh_token, icon, memory_reservation, memory_limit, cpu_reservation, cpu_limit, replicas, health_check_swarm, restart_policy_swarm, placement_swarm, update_config_swarm, rollback_config_swarm, mode_swarm, labels_swarm, network_swarm, endpoint_spec_swarm, ulimits_swarm, stop_grace_period_swarm, repository, owner, branch, auto_deploy, gitlab_project_id, gitlab_repository, gitlab_owner, gitlab_branch, gitlab_build_path, gitlab_path_namespace, gitea_repository, gitea_owner, gitea_branch, gitea_build_path, bitbucket_repository, bitbucket_repository_slug, bitbucket_owner, bitbucket_branch, bitbucket_build_path, docker_image, docker_username, docker_password, registry_url, custom_git_url, custom_git_branch, custom_git_build_path, custom_git_ssh_key_id, preview_env, preview_build_args, preview_build_secrets, preview_labels, preview_wildcard, preview_port, preview_https, preview_path, preview_certificate_type, preview_custom_cert_resolver, preview_limit, is_preview_deployments_active, preview_require_collaborator_permissions, rollback_active, environment_id, server_id, build_server_id, registry_id, rollback_registry_id, build_registry_id, github_provider_id, gitlab_provider_id, gitea_provider_id, bitbucket_provider_id, created_at, updated_at",
                values_parts.join(", ")
            );
            let mut query = sqlx::query_as::<_, Application>(&sql);
            for params in chunk {
                query = query.bind(&params.name);
                query = query.bind(&params.app_name);
                query = query.bind(&params.description);
                query = query.bind(&params.source_type);
                query = query.bind(&params.build_type);
                query = query.bind(&params.app_status);
                query = query.bind(&params.trigger_type);
                query = query.bind(&params.build_args);
                query = query.bind(&params.build_secrets);
                query = query.bind(&params.dockerfile);
                query = query.bind(&params.docker_context_path);
                query = query.bind(&params.docker_build_stage);
                query = query.bind(&params.publish_directory);
                query = query.bind(&params.is_static_spa);
                query = query.bind(&params.create_env_file);
                query = query.bind(&params.railpack_version);
                query = query.bind(&params.heroku_version);
                query = query.bind(&params.command);
                query = query.bind(&params.args);
                query = query.bind(&params.env_var);
                query = query.bind(&params.build_path);
                query = query.bind(&params.clean_cache);
                query = query.bind(&params.drop_build_path);
                query = query.bind(&params.enable_submodules);
                query = query.bind(&params.watch_paths);
                query = query.bind(&params.refresh_token);
                query = query.bind(&params.icon);
                query = query.bind(&params.memory_reservation);
                query = query.bind(&params.memory_limit);
                query = query.bind(&params.cpu_reservation);
                query = query.bind(&params.cpu_limit);
                query = query.bind(&params.replicas);
                query = query.bind(&params.health_check_swarm);
                query = query.bind(&params.restart_policy_swarm);
                query = query.bind(&params.placement_swarm);
                query = query.bind(&params.update_config_swarm);
                query = query.bind(&params.rollback_config_swarm);
                query = query.bind(&params.mode_swarm);
                query = query.bind(&params.labels_swarm);
                query = query.bind(&params.network_swarm);
                query = query.bind(&params.endpoint_spec_swarm);
                query = query.bind(&params.ulimits_swarm);
                query = query.bind(&params.stop_grace_period_swarm);
                query = query.bind(&params.repository);
                query = query.bind(&params.owner);
                query = query.bind(&params.branch);
                query = query.bind(&params.auto_deploy);
                query = query.bind(&params.gitlab_project_id);
                query = query.bind(&params.gitlab_repository);
                query = query.bind(&params.gitlab_owner);
                query = query.bind(&params.gitlab_branch);
                query = query.bind(&params.gitlab_build_path);
                query = query.bind(&params.gitlab_path_namespace);
                query = query.bind(&params.gitea_repository);
                query = query.bind(&params.gitea_owner);
                query = query.bind(&params.gitea_branch);
                query = query.bind(&params.gitea_build_path);
                query = query.bind(&params.bitbucket_repository);
                query = query.bind(&params.bitbucket_repository_slug);
                query = query.bind(&params.bitbucket_owner);
                query = query.bind(&params.bitbucket_branch);
                query = query.bind(&params.bitbucket_build_path);
                query = query.bind(&params.docker_image);
                query = query.bind(&params.docker_username);
                query = query.bind(&params.docker_password);
                query = query.bind(&params.registry_url);
                query = query.bind(&params.custom_git_url);
                query = query.bind(&params.custom_git_branch);
                query = query.bind(&params.custom_git_build_path);
                query = query.bind(&params.custom_git_ssh_key_id);
                query = query.bind(&params.preview_env);
                query = query.bind(&params.preview_build_args);
                query = query.bind(&params.preview_build_secrets);
                query = query.bind(&params.preview_labels);
                query = query.bind(&params.preview_wildcard);
                query = query.bind(&params.preview_port);
                query = query.bind(&params.preview_https);
                query = query.bind(&params.preview_path);
                query = query.bind(&params.preview_certificate_type);
                query = query.bind(&params.preview_custom_cert_resolver);
                query = query.bind(&params.preview_limit);
                query = query.bind(&params.is_preview_deployments_active);
                query = query.bind(&params.preview_require_collaborator_permissions);
                query = query.bind(&params.rollback_active);
                query = query.bind(&params.environment_id);
                query = query.bind(&params.server_id);
                query = query.bind(&params.build_server_id);
                query = query.bind(&params.registry_id);
                query = query.bind(&params.rollback_registry_id);
                query = query.bind(&params.build_registry_id);
                query = query.bind(&params.github_provider_id);
                query = query.bind(&params.gitlab_provider_id);
                query = query.bind(&params.gitea_provider_id);
                query = query.bind(&params.bitbucket_provider_id);
                query = query.bind(&params.created_at);
                query = query.bind(&params.updated_at);
            }
            let rows = query.fetch_all(&mut *tx).await?;
            all_results.extend(rows);
        }
        tx.commit().await?;

        Ok(all_results)
    }

    pub async fn overwrite(
        &self,
        id: String,
        params: &OverwriteApplicationParams,
    ) -> Result<Application, sqlx::Error> {
        sqlx::query_as::<_, Application>(
            r#"
            UPDATE applications
            SET
              name = ?,
              app_name = ?,
              description = ?,
              source_type = ?,
              build_type = ?,
              app_status = ?,
              trigger_type = ?,
              build_args = ?,
              build_secrets = ?,
              dockerfile = ?,
              docker_context_path = ?,
              docker_build_stage = ?,
              publish_directory = ?,
              is_static_spa = ?,
              create_env_file = ?,
              railpack_version = ?,
              heroku_version = ?,
              command = ?,
              args = ?,
              env_var = ?,
              build_path = ?,
              clean_cache = ?,
              drop_build_path = ?,
              enable_submodules = ?,
              watch_paths = ?,
              refresh_token = ?,
              icon = ?,
              memory_reservation = ?,
              memory_limit = ?,
              cpu_reservation = ?,
              cpu_limit = ?,
              replicas = ?,
              health_check_swarm = ?,
              restart_policy_swarm = ?,
              placement_swarm = ?,
              update_config_swarm = ?,
              rollback_config_swarm = ?,
              mode_swarm = ?,
              labels_swarm = ?,
              network_swarm = ?,
              endpoint_spec_swarm = ?,
              ulimits_swarm = ?,
              stop_grace_period_swarm = ?,
              repository = ?,
              owner = ?,
              branch = ?,
              auto_deploy = ?,
              gitlab_project_id = ?,
              gitlab_repository = ?,
              gitlab_owner = ?,
              gitlab_branch = ?,
              gitlab_build_path = ?,
              gitlab_path_namespace = ?,
              gitea_repository = ?,
              gitea_owner = ?,
              gitea_branch = ?,
              gitea_build_path = ?,
              bitbucket_repository = ?,
              bitbucket_repository_slug = ?,
              bitbucket_owner = ?,
              bitbucket_branch = ?,
              bitbucket_build_path = ?,
              docker_image = ?,
              docker_username = ?,
              docker_password = ?,
              registry_url = ?,
              custom_git_url = ?,
              custom_git_branch = ?,
              custom_git_build_path = ?,
              custom_git_ssh_key_id = ?,
              preview_env = ?,
              preview_build_args = ?,
              preview_build_secrets = ?,
              preview_labels = ?,
              preview_wildcard = ?,
              preview_port = ?,
              preview_https = ?,
              preview_path = ?,
              preview_certificate_type = ?,
              preview_custom_cert_resolver = ?,
              preview_limit = ?,
              is_preview_deployments_active = ?,
              preview_require_collaborator_permissions = ?,
              rollback_active = ?,
              environment_id = ?,
              server_id = ?,
              build_server_id = ?,
              registry_id = ?,
              rollback_registry_id = ?,
              build_registry_id = ?,
              github_provider_id = ?,
              gitlab_provider_id = ?,
              gitea_provider_id = ?,
              bitbucket_provider_id = ?,
              created_at = ?,
              updated_at = ?
            WHERE id = ?
            RETURNING *
        "#,
        )
        .bind(&params.name)
        .bind(&params.app_name)
        .bind(&params.description)
        .bind(&params.source_type)
        .bind(&params.build_type)
        .bind(&params.app_status)
        .bind(&params.trigger_type)
        .bind(&params.build_args)
        .bind(&params.build_secrets)
        .bind(&params.dockerfile)
        .bind(&params.docker_context_path)
        .bind(&params.docker_build_stage)
        .bind(&params.publish_directory)
        .bind(&params.is_static_spa)
        .bind(&params.create_env_file)
        .bind(&params.railpack_version)
        .bind(&params.heroku_version)
        .bind(&params.command)
        .bind(&params.args)
        .bind(&params.env_var)
        .bind(&params.build_path)
        .bind(&params.clean_cache)
        .bind(&params.drop_build_path)
        .bind(&params.enable_submodules)
        .bind(&params.watch_paths)
        .bind(&params.refresh_token)
        .bind(&params.icon)
        .bind(&params.memory_reservation)
        .bind(&params.memory_limit)
        .bind(&params.cpu_reservation)
        .bind(&params.cpu_limit)
        .bind(&params.replicas)
        .bind(&params.health_check_swarm)
        .bind(&params.restart_policy_swarm)
        .bind(&params.placement_swarm)
        .bind(&params.update_config_swarm)
        .bind(&params.rollback_config_swarm)
        .bind(&params.mode_swarm)
        .bind(&params.labels_swarm)
        .bind(&params.network_swarm)
        .bind(&params.endpoint_spec_swarm)
        .bind(&params.ulimits_swarm)
        .bind(&params.stop_grace_period_swarm)
        .bind(&params.repository)
        .bind(&params.owner)
        .bind(&params.branch)
        .bind(&params.auto_deploy)
        .bind(&params.gitlab_project_id)
        .bind(&params.gitlab_repository)
        .bind(&params.gitlab_owner)
        .bind(&params.gitlab_branch)
        .bind(&params.gitlab_build_path)
        .bind(&params.gitlab_path_namespace)
        .bind(&params.gitea_repository)
        .bind(&params.gitea_owner)
        .bind(&params.gitea_branch)
        .bind(&params.gitea_build_path)
        .bind(&params.bitbucket_repository)
        .bind(&params.bitbucket_repository_slug)
        .bind(&params.bitbucket_owner)
        .bind(&params.bitbucket_branch)
        .bind(&params.bitbucket_build_path)
        .bind(&params.docker_image)
        .bind(&params.docker_username)
        .bind(&params.docker_password)
        .bind(&params.registry_url)
        .bind(&params.custom_git_url)
        .bind(&params.custom_git_branch)
        .bind(&params.custom_git_build_path)
        .bind(&params.custom_git_ssh_key_id)
        .bind(&params.preview_env)
        .bind(&params.preview_build_args)
        .bind(&params.preview_build_secrets)
        .bind(&params.preview_labels)
        .bind(&params.preview_wildcard)
        .bind(&params.preview_port)
        .bind(&params.preview_https)
        .bind(&params.preview_path)
        .bind(&params.preview_certificate_type)
        .bind(&params.preview_custom_cert_resolver)
        .bind(&params.preview_limit)
        .bind(&params.is_preview_deployments_active)
        .bind(&params.preview_require_collaborator_permissions)
        .bind(&params.rollback_active)
        .bind(&params.environment_id)
        .bind(&params.server_id)
        .bind(&params.build_server_id)
        .bind(&params.registry_id)
        .bind(&params.rollback_registry_id)
        .bind(&params.build_registry_id)
        .bind(&params.github_provider_id)
        .bind(&params.gitlab_provider_id)
        .bind(&params.gitea_provider_id)
        .bind(&params.bitbucket_provider_id)
        .bind(&params.created_at)
        .bind(&params.updated_at)
        .bind(id)
        .fetch_one(&self.pool)
        .await
    }

    pub async fn update(
        &self,
        id: String,
        params: &UpdateApplicationParams,
    ) -> Result<Application, sqlx::Error> {
        sqlx::query_as::<
            _,
            Application,
        >(
                r#"
            UPDATE applications
            SET
              name = COALESCE(?, name),
              app_name = COALESCE(?, app_name),
              description = COALESCE(?, description),
              source_type = COALESCE(?, source_type),
              build_type = COALESCE(?, build_type),
              app_status = COALESCE(?, app_status),
              trigger_type = COALESCE(?, trigger_type),
              build_args = COALESCE(?, build_args),
              build_secrets = COALESCE(?, build_secrets),
              dockerfile = COALESCE(?, dockerfile),
              docker_context_path = COALESCE(?, docker_context_path),
              docker_build_stage = COALESCE(?, docker_build_stage),
              publish_directory = COALESCE(?, publish_directory),
              is_static_spa = COALESCE(?, is_static_spa),
              create_env_file = COALESCE(?, create_env_file),
              railpack_version = COALESCE(?, railpack_version),
              heroku_version = COALESCE(?, heroku_version),
              command = COALESCE(?, command),
              args = COALESCE(?, args),
              env_var = COALESCE(?, env_var),
              build_path = COALESCE(?, build_path),
              clean_cache = COALESCE(?, clean_cache),
              drop_build_path = COALESCE(?, drop_build_path),
              enable_submodules = COALESCE(?, enable_submodules),
              watch_paths = COALESCE(?, watch_paths),
              refresh_token = COALESCE(?, refresh_token),
              icon = COALESCE(?, icon),
              memory_reservation = COALESCE(?, memory_reservation),
              memory_limit = COALESCE(?, memory_limit),
              cpu_reservation = COALESCE(?, cpu_reservation),
              cpu_limit = COALESCE(?, cpu_limit),
              replicas = COALESCE(?, replicas),
              health_check_swarm = COALESCE(?, health_check_swarm),
              restart_policy_swarm = COALESCE(?, restart_policy_swarm),
              placement_swarm = COALESCE(?, placement_swarm),
              update_config_swarm = COALESCE(?, update_config_swarm),
              rollback_config_swarm = COALESCE(?, rollback_config_swarm),
              mode_swarm = COALESCE(?, mode_swarm),
              labels_swarm = COALESCE(?, labels_swarm),
              network_swarm = COALESCE(?, network_swarm),
              endpoint_spec_swarm = COALESCE(?, endpoint_spec_swarm),
              ulimits_swarm = COALESCE(?, ulimits_swarm),
              stop_grace_period_swarm = COALESCE(?, stop_grace_period_swarm),
              repository = COALESCE(?, repository),
              owner = COALESCE(?, owner),
              branch = COALESCE(?, branch),
              auto_deploy = COALESCE(?, auto_deploy),
              gitlab_project_id = COALESCE(?, gitlab_project_id),
              gitlab_repository = COALESCE(?, gitlab_repository),
              gitlab_owner = COALESCE(?, gitlab_owner),
              gitlab_branch = COALESCE(?, gitlab_branch),
              gitlab_build_path = COALESCE(?, gitlab_build_path),
              gitlab_path_namespace = COALESCE(?, gitlab_path_namespace),
              gitea_repository = COALESCE(?, gitea_repository),
              gitea_owner = COALESCE(?, gitea_owner),
              gitea_branch = COALESCE(?, gitea_branch),
              gitea_build_path = COALESCE(?, gitea_build_path),
              bitbucket_repository = COALESCE(?, bitbucket_repository),
              bitbucket_repository_slug = COALESCE(?, bitbucket_repository_slug),
              bitbucket_owner = COALESCE(?, bitbucket_owner),
              bitbucket_branch = COALESCE(?, bitbucket_branch),
              bitbucket_build_path = COALESCE(?, bitbucket_build_path),
              docker_image = COALESCE(?, docker_image),
              docker_username = COALESCE(?, docker_username),
              docker_password = COALESCE(?, docker_password),
              registry_url = COALESCE(?, registry_url),
              custom_git_url = COALESCE(?, custom_git_url),
              custom_git_branch = COALESCE(?, custom_git_branch),
              custom_git_build_path = COALESCE(?, custom_git_build_path),
              custom_git_ssh_key_id = COALESCE(?, custom_git_ssh_key_id),
              preview_env = COALESCE(?, preview_env),
              preview_build_args = COALESCE(?, preview_build_args),
              preview_build_secrets = COALESCE(?, preview_build_secrets),
              preview_labels = COALESCE(?, preview_labels),
              preview_wildcard = COALESCE(?, preview_wildcard),
              preview_port = COALESCE(?, preview_port),
              preview_https = COALESCE(?, preview_https),
              preview_path = COALESCE(?, preview_path),
              preview_certificate_type = COALESCE(?, preview_certificate_type),
              preview_custom_cert_resolver = COALESCE(?, preview_custom_cert_resolver),
              preview_limit = COALESCE(?, preview_limit),
              is_preview_deployments_active = COALESCE(?, is_preview_deployments_active),
              preview_require_collaborator_permissions = COALESCE(?, preview_require_collaborator_permissions),
              rollback_active = COALESCE(?, rollback_active),
              environment_id = COALESCE(?, environment_id),
              server_id = COALESCE(?, server_id),
              build_server_id = COALESCE(?, build_server_id),
              registry_id = COALESCE(?, registry_id),
              rollback_registry_id = COALESCE(?, rollback_registry_id),
              build_registry_id = COALESCE(?, build_registry_id),
              github_provider_id = COALESCE(?, github_provider_id),
              gitlab_provider_id = COALESCE(?, gitlab_provider_id),
              gitea_provider_id = COALESCE(?, gitea_provider_id),
              bitbucket_provider_id = COALESCE(?, bitbucket_provider_id),
              created_at = COALESCE(?, created_at),
              updated_at = COALESCE(?, updated_at)
            WHERE id = ?
            RETURNING *
        "#,
            )
            .bind(&params.name)
            .bind(&params.app_name)
            .bind(&params.description)
            .bind(&params.source_type)
            .bind(&params.build_type)
            .bind(&params.app_status)
            .bind(&params.trigger_type)
            .bind(&params.build_args)
            .bind(&params.build_secrets)
            .bind(&params.dockerfile)
            .bind(&params.docker_context_path)
            .bind(&params.docker_build_stage)
            .bind(&params.publish_directory)
            .bind(&params.is_static_spa)
            .bind(&params.create_env_file)
            .bind(&params.railpack_version)
            .bind(&params.heroku_version)
            .bind(&params.command)
            .bind(&params.args)
            .bind(&params.env_var)
            .bind(&params.build_path)
            .bind(&params.clean_cache)
            .bind(&params.drop_build_path)
            .bind(&params.enable_submodules)
            .bind(&params.watch_paths)
            .bind(&params.refresh_token)
            .bind(&params.icon)
            .bind(&params.memory_reservation)
            .bind(&params.memory_limit)
            .bind(&params.cpu_reservation)
            .bind(&params.cpu_limit)
            .bind(&params.replicas)
            .bind(&params.health_check_swarm)
            .bind(&params.restart_policy_swarm)
            .bind(&params.placement_swarm)
            .bind(&params.update_config_swarm)
            .bind(&params.rollback_config_swarm)
            .bind(&params.mode_swarm)
            .bind(&params.labels_swarm)
            .bind(&params.network_swarm)
            .bind(&params.endpoint_spec_swarm)
            .bind(&params.ulimits_swarm)
            .bind(&params.stop_grace_period_swarm)
            .bind(&params.repository)
            .bind(&params.owner)
            .bind(&params.branch)
            .bind(&params.auto_deploy)
            .bind(&params.gitlab_project_id)
            .bind(&params.gitlab_repository)
            .bind(&params.gitlab_owner)
            .bind(&params.gitlab_branch)
            .bind(&params.gitlab_build_path)
            .bind(&params.gitlab_path_namespace)
            .bind(&params.gitea_repository)
            .bind(&params.gitea_owner)
            .bind(&params.gitea_branch)
            .bind(&params.gitea_build_path)
            .bind(&params.bitbucket_repository)
            .bind(&params.bitbucket_repository_slug)
            .bind(&params.bitbucket_owner)
            .bind(&params.bitbucket_branch)
            .bind(&params.bitbucket_build_path)
            .bind(&params.docker_image)
            .bind(&params.docker_username)
            .bind(&params.docker_password)
            .bind(&params.registry_url)
            .bind(&params.custom_git_url)
            .bind(&params.custom_git_branch)
            .bind(&params.custom_git_build_path)
            .bind(&params.custom_git_ssh_key_id)
            .bind(&params.preview_env)
            .bind(&params.preview_build_args)
            .bind(&params.preview_build_secrets)
            .bind(&params.preview_labels)
            .bind(&params.preview_wildcard)
            .bind(&params.preview_port)
            .bind(&params.preview_https)
            .bind(&params.preview_path)
            .bind(&params.preview_certificate_type)
            .bind(&params.preview_custom_cert_resolver)
            .bind(&params.preview_limit)
            .bind(&params.is_preview_deployments_active)
            .bind(&params.preview_require_collaborator_permissions)
            .bind(&params.rollback_active)
            .bind(&params.environment_id)
            .bind(&params.server_id)
            .bind(&params.build_server_id)
            .bind(&params.registry_id)
            .bind(&params.rollback_registry_id)
            .bind(&params.build_registry_id)
            .bind(&params.github_provider_id)
            .bind(&params.gitlab_provider_id)
            .bind(&params.gitea_provider_id)
            .bind(&params.bitbucket_provider_id)
            .bind(&params.created_at)
            .bind(&params.updated_at)
            .bind(id)
            .fetch_one(&self.pool)
            .await
    }

    pub async fn delete(&self, id: String) -> Result<(), sqlx::Error> {
        sqlx::query(
            r#"
            DELETE FROM applications
            WHERE id = ?
        "#,
        )
        .bind(id)
        .execute(&self.pool)
        .await?;

        Ok(())
    }
}
